# 📚 Лабораторная работа №4 (Вариант 7)

## Структура метода run()
### Основной функционал
run() запускает цикл, принимает от пользователя номер задачи и запускает её, затем предлагает решить еще,
пока пользователь не введет "0".

### Цикл while(true)
Цикл начинается в начале программы и ожидает ввод пользователем номера задачи, затем с помощью switch создает соответствующий объект задачи и запускает его.
Действия повторяются пока пользователь не введет "0".

### Справочная информация для пользователя
Перед началом цикла выводится информация для пользователя, сообщающая о формате ввода задач и как закончить работу программы.

## Общая структура всех задач
Все задачи из заданий имеют следующую структуру:

| Метод                        | Назначение                                                                     |
|------------------------------|--------------------------------------------------------------------------------|
| `public void runTask...()`   | Принятие ввода, создание переменных, запуск нужных методов, вывод результата   |
| `public class <имя>`         | класс(ы) использующиеся в задании                                              |
| `public <класс>`             | конструктор(ы)                                                                 |
| `public <метод>`             | геттер(ы) и сеттер(ы) класса                                                   |
| `public String toString()`   | перегруженный метод toString() выводит содержание полей класса в строковом виде|

## Обработка ошибок
- На этапе инициализации некоторых классов учтена корректность данных
- При попытке положить объект в заполненную коробку выбрасывается исключение
- Обработка null значений в комбинаторах

## Класс Box<T> (задание 1.1)

### Поля класса Box
Box имеет 1 поле:
- `private T thing` - объект, хранящийся в коробке

### Конструкторы
Box имеет 2 конструктора:
- `public Box()` - создает пустую коробку
- `public Box(T thing)` - создает коробку с заданным объектом

### Методы
- `public T getThing()` - возвращает объект из коробки
- `public void setThing(T thing)` - помещает объект в коробку (если коробка не пуста - исключение)
- `public void removeThing()` - удаляет объект из коробки
- `public boolean isBoxFull()` - проверяет, заполнена ли коробка
- `@Override public String toString()` - возвращает строковое представление содержимого коробки

## Класс Storage<T> (задание 1.2)

### Поля класса Storage
Storage имеет 1 поле:
- `private final T item` - финальный объект хранилища

### Конструктор
Storage имеет 1 конструктор:
- `public Storage(T item)` - создает хранилище с заданным объектом

### Методы
- `public T getRawItem()` - возвращает объект хранилища (может быть null)
- `public T getItem(T alternative)` - возвращает объект хранилища или альтернативное значение если null
- `public boolean isStorageFull()` - проверяет, не является ли объект null
- `@Override public String toString()` - возвращает строковое представление содержимого хранилища

## Интерфейс Function<T,P> (задание 3.1)

### Методы интерфейса
Function объявляет 1 метод:
- `P apply(T argument)` - применяет функцию к аргументу и возвращает результат

---

## Класс StringLength (задание 3.1) - реализует Function<String, Integer>

### Конструктор
StringLength имеет конструктор по умолчанию

### Методы
- `@Override public Integer apply(String argument)` - возвращает длину строки

---

## Класс Abs (задание 3.1) - реализует Function<Integer, Integer>

### Конструктор
Abs имеет конструктор по умолчанию

### Методы
- `@Override public Integer apply(Integer argument)` - возвращает абсолютное значение числа

---

## Класс MaxFromList (задание 3.1) - реализует Function<int[], Integer>

### Конструктор
MaxFromList имеет конструктор по умолчанию

### Методы
- `@Override public Integer apply(int[] arr)` - возвращает максимальное значение из массива

---

## Класс ApplyFunctionToList (задание 3.1)

### Методы
- `public static <T, P> List<P> applyFunctionToList(List<T> array, Function<T, P> function)` - применяет функцию к каждому элементу списка и возвращает новый список
- Обрабатывает null параметры, возвращая пустой список
- Типы T и P могут быть разными

---


## Интерфейс Filter<T> (задание 3.2)

### Методы интерфейса
Filter объявляет 1 метод:
- `boolean test(T argument)` - проверяет условие для аргумента

---

## Класс LengthFilter (задание 3.2) - реализует Filter<String>

### Конструктор
LengthFilter имеет конструктор по умолчанию

### Методы
- `@Override public boolean test(String argument)` - проверяет, что длина строки ≥ 3 символов

---

## Класс PositiveFilter (задание 3.2) - реализует Filter<Integer>

### Конструктор
PositiveFilter имеет конструктор по умолчанию

### Методы
- `@Override public boolean test(Integer argument)` - проверяет, что число ≥ 0

---

## Класс PositiveArrayFilter (задание 3.2) - реализует Filter<List<Integer>>

### Конструктор
PositiveArrayFilter имеет конструктор по умолчанию

### Методы
- `@Override public boolean test(List<Integer> argument)` - проверяет, что все элементы списка ≥ 0

---

## Класс FilteringList (задание 3.2)

### Методы
- `public static <T> List<T> filteringList(List<T> array, Filter<T> filter)` - фильтрует список по заданному условию
- Обрабатывает null параметры, возвращая пустой список
- Возвращает только те элементы, которые удовлетворяют условию фильтра

---

## Интерфейс Combiner<T> (задание 3.3)

### Методы интерфейса
Combiner объявляет 1 метод:
- `T combine(List<T> list)` - объединяет список значений в одно значение

---

## Класс StringCombiner (задание 3.3) - реализует Combiner<String>

### Конструктор
StringCombiner имеет конструктор по умолчанию

### Методы
- `@Override public String combine(List<String> list)` - объединяет все строки списка в одну
- Обрабатывает null список, возвращая пустую строку

---

## Класс IntSumCombiner (задание 3.3) - реализует Combiner<Integer>

### Конструктор
IntSumCombiner имеет конструктор по умолчанию

### Методы
- `@Override public Integer combine(List<Integer> list)` - возвращает сумму всех чисел списка
- Обрабатывает null список, возвращая 0

---

## Класс ArrayCountElementsCombiner (задание 3.3) - реализует Combiner<List<Integer>>

### Конструктор
ArrayCountElementsCombiner имеет конструктор по умолчанию

### Методы
- `@Override public List<Integer> combine(List<List<Integer>> list)` - возвращает сумму всех элементов всех списков
- Обрабатывает null список, возвращая список с одним элементом 0

---
## Интерфейс CollectionFactory<P> (задание 3.4)

### Методы интерфейса
CollectionFactory объявляет 1 метод:
- `P create()` - создает новую коллекцию указанного типа

---

## Интерфейс ElementAdder<T, P> (задание 3.4)

### Методы интерфейса
ElementAdder объявляет 1 метод:
- `void add(P collection, T element)` - добавляет элемент в коллекцию

---

## Класс CollectList (задание 3.4)

### Методы
- `public static <T, P> P collect(List<T> sourceList, CollectionFactory<P> collectionFactory, ElementAdder<T, P> elementAdder)` - собирает коллекцию из исходного списка
- Обрабатывает null исходный список, выбрасывая исключение

---

## Класс NumberPartitionFactory (задание 3.4) - реализует CollectionFactory<Map<String, List<Integer>>>

### Конструктор
NumberPartitionFactory имеет конструктор по умолчанию

### Методы
- `@Override public Map<String, List<Integer>> create()` - создает карту с ключами "positive" и "negative"

---

## Класс NumberPartitionAdder (задание 3.4) - реализует ElementAdder<Integer, Map<String, List<Integer>>>

### Конструктор
NumberPartitionAdder имеет конструктор по умолчанию

### Методы
- `@Override public void add(Map<String, List<Integer>> map, Integer number)` - добавляет число в соответствующий список (положительные/отрицательные)

---

## Класс NumberPartitioner (задание 3.4)

### Поля класса
NumberPartitioner имеет 2 статических поля:
- `private static final NumberPartitionFactory FACTORY` - фабрика для создания карты
- `private static final NumberPartitionAdder ADDER` - добавщик для чисел

### Методы
- `public static Map<String, List<Integer>> partitionNumbers(List<Integer> numbers)` - разделяет числа на положительные и отрицательные
- Обрабатывает null список, возвращая пустую карту

---

## Класс StringLengthGroupFactory (задание 3.4) - реализует CollectionFactory<Map<Integer, List<String>>>

### Конструктор
StringLengthGroupFactory имеет конструктор по умолчанию

### Методы
- `@Override public Map<Integer, List<String>> create()` - создает пустую карту для группировки строк по длине

---

## Класс StringLengthGroupAdder (задание 3.4) - реализует ElementAdder<String, Map<Integer, List<String>>>

### Конструктор
StringLengthGroupAdder имеет конструктор по умолчанию

### Методы
- `@Override public void add(Map<Integer, List<String>> map, String str)` - добавляет строку в список соответствующей длины

---

## Класс StringLengthGrouper (задание 3.4)

### Поля класса
StringLengthGrouper имеет 2 статических поля:
- `private static final StringLengthGroupFactory FACTORY` - фабрика для создания карты
- `private static final StringLengthGroupAdder ADDER` - добавщик для строк

### Методы
- `public static Map<Integer, List<String>> groupByLength(List<String> strings)` - группирует строки по их длине
- Обрабатывает null список, возвращая пустую карту

---

## Класс UniqueStringFactory (задание 3.4) - реализует CollectionFactory<Set<String>>

### Конструктор
UniqueStringFactory имеет конструктор по умолчанию

### Методы
- `@Override public Set<String> create()` - создает пустой набор для уникальных строк

---

## Класс UniqueStringAdder (задание 3.4) - реализует ElementAdder<String, Set<String>>

### Конструктор
UniqueStringAdder имеет конструктор по умолчанию

### Методы
- `@Override public void add(Set<String> set, String str)` - добавляет строку в набор (дубликаты автоматически игнорируются)

---

## Класс UniqueStringCollector (задание 3.4)

### Поля класса
UniqueStringCollector имеет 2 статических поля:
- `private static final UniqueStringFactory FACTORY` - фабрика для создания набора
- `private static final UniqueStringAdder ADDER` - добавщик для строк

### Методы
- `public static Set<String> collectUnique(List<String> strings)` - собирает уникальные строки из списка
- Обрабатывает null список, возвращая пустой набор

---

## Обобщенные методы Lab4

### Метод printBox
- `private <T> void printBox(Box<T> box)` - выводит содержимое коробки любого типа

### Метод printStorage
- `public <T> void printStorage(Storage<T> storage, T alternative)` - выводит содержимое хранилища с альтернативным значением

### Метод putPointInBox
- `private void putPointInBox(Box<? super Point2D> box)` - принимает коробку с Point2D или ее предками

## Тесты
## Задание 1.1 - Обобщенная коробка

### Тест 1: Создание коробки с целым числом
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Box<Integer> box = new Box<>(); box.setThing(3);` | Коробка содержит: 3 |

### Тест 2: Создание коробки со строкой
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Box<String> box2 = new Box<>(); box2.setThing("РОССИЯ ВПЕРЕД!!!");` | Коробка содержит: "РОССИЯ ВПЕРЕД!!!" |

### Тест 3: Попытка положить объект в заполненную коробку
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `box.setThing(5);` когда коробка уже содержит 3 | Исключение: "Коробка уже содержит объект!" |

## Задание 1.2 - Хранилище без null

### Тест 1: Хранилище чисел с null
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Storage<Integer> storage1 = new Storage<>(null); printStorage(storage1, 0);` | Вывод: 0 |

### Тест 2: Хранилище чисел с значением
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Storage<Integer> storage2 = new Storage<>(99); printStorage(storage2, -1);` | Вывод: 99 |

### Тест 3: Хранилище строк с null
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Storage<String> storage3 = new Storage<>(null); printStorage(storage3, "default");` | Вывод: "default" |

### Тест 4: Хранилище строк со значением
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Storage<String> storage4 = new Storage<>("hello"); printStorage(storage4, "hello world");` | Вывод: "hello" |

## Задание 2.3 - Начало отсчета

### Тест 1: Коробка с Point2D
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Box<? super Point2D> box1 = new Box<>(new Point2D(1,2));` | Коробка содержит: {1 ; 2} |

### Тест 2: Коробка с Point3D
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Box<? super Point2D> box2 = new Box<>(new Point3D(1,2,3));` | Коробка содержит: {1 ; 2 ; 3} |

## Задание 3.1 - Функция

### Тест 1: Длина строк
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `ApplyFunctionToList.applyFunctionToList(List.of("qwerty", "asdfg", "zx"), new StringLength())` | [6, 5, 2] |

### Тест 2: Абсолютные значения чисел
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `ApplyFunctionToList.applyFunctionToList(List.of(1, -3, 7), new Abs())` | [1, 3, 7] |

### Тест 3: Максимумы массивов
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `ApplyFunctionToList.applyFunctionToList(List.of(new int[]{1,2,3}, new int[]{1,-2,-34}, new int[]{10,32,3}), new MaxFromList())` | [3, 1, 32] |

### Тест 4: Обработка null массива в ApplyFunctionToList
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `ApplyFunctionToList.applyFunctionToList(null, new StringLength())` | [] |

### Тест 5: Обработка null функции в ApplyFunctionToList
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `ApplyFunctionToList.applyFunctionToList(List.of("a", "b"), null)` | [] |

## Задание 3.2 - Фильтр

### Тест 1: Фильтр длины строк
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `FilteringList.filteringList(List.of("qwerty", "asdfg", "zx"), new LengthFilter())` | ["qwerty", "asdfg"] |

### Тест 2: Фильтр положительных чисел
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `FilteringList.filteringList(List.of(1, -3, 7), new PositiveFilter())` | [1, 7] |

### Тест 3: Фильтр массивов без положительных элементов
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `FilteringList.filteringList(List.of(List.of(10,3,4), List.of(12,5,7), List.of(10,-3,4)), new PositiveArrayFilter())` | [ ] |

### Тест 4: Обработка null массива в FilteringList
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `FilteringList.filteringList(null, new LengthFilter())` | [] |

### Тест 5: Обработка null фильтра в FilteringList
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `FilteringList.filteringList(List.of("a", "b"), null)` | [] |

### Тест 6: Фильтр пустого списка
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `FilteringList.filteringList(List.of(), new LengthFilter())` | [] |

## Задание 3.3 - Сокращение

### Тест 1: Комбинация строк
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `new StringCombiner().combine(List.of("qwerty", "asdfg", "zx"))` | "qwertyasdfgzx" |

### Тест 2: Сумма чисел
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `new IntSumCombiner().combine(List.of(1, -3, 7))` | 5 |

### Тест 3: Количество элементов в списках
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `new ArrayCountElementsCombiner().combine(List.of(List.of(10,3,4), List.of(12,5,7), List.of(10,-3,4)))` | [9] |

### Тест 4: Обработка null списка
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `new ArrayCountElementsCombiner().combine(null)` | [0] |

### Тест 5: Обработка null списка в StringCombiner
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `new StringCombiner().combine(null)` | "" |

### Тест 6: Обработка null списка в IntSumCombiner
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `new IntSumCombiner().combine(null)` | 0 |

## Задание 3.4 - Коллекционирование

### Тест 1: Разделение чисел на положительные и отрицательные
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `NumberPartitioner.partitionNumbers(List.of(1, -3, 7, -2, 5))` | {positive=[1, 7, 5], negative=[-3, -2]} |

### Тест 2: Обработка null списка в NumberPartitioner
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `NumberPartitioner.partitionNumbers(null)` | {} |

### Тест 3: Группировка строк по длине
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `StringLengthGrouper.groupByLength(List.of("qwerty", "asdfg", "zx", "qw"))` | {6=["qwerty"], 5=["asdfg"], 2=["zx", "qw"]} |

### Тест 4: Обработка null списка в StringLengthGrouper
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `StringLengthGrouper.groupByLength(null)` | {} |

### Тест 5: Сбор уникальных строк
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `UniqueStringCollector.collectUnique(List.of("qwerty", "asdfg", "qwerty", "qw"))` | ["qwerty", "asdfg", "qw"] |

### Тест 6: Обработка null списка в UniqueStringCollector
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `UniqueStringCollector.collectUnique(null)` | [] |

### Тест 7: Обработка исключения в CollectList
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `CollectList.collect(null, factory, adder)` | Исключение: "Исходный список null" |
