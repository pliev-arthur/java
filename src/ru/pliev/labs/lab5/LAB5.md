# 📚 Лабораторная работа №5 (Вариант 7)

## Структура метода run()
### Основной функционал
run() запускает цикл, принимает от пользователя номер задачи и запускает её, затем предлагает решить еще, пока пользователь не введет "0".

### Цикл while(true)
Цикл начинается в начале программы и ожидает ввод пользователем номера задачи, затем с помощью switch создает соответствующий объект задачи и запускает его. Действия повторяются пока пользователь не введет "0".

### Справочная информация для пользователя
Перед началом цикла выводится информация для пользователя, сообщающая о формате ввода задач и как закончить работу программы.

## Общая структура всех задач
Все задачи из заданий имеют следующую структуру:

| Метод                        | Назначение                                                                     |
|------------------------------|--------------------------------------------------------------------------------|
| `public void runTask...()`   | Принятие ввода, создание переменных, запуск нужных методов, вывод результата   |
| `public class <имя>`         | класс(ы) использующиеся в задании                                              |
| `public <класс>`             | конструктор(ы)                                                                 |
| `public <метод>`             | геттер(ы) и сеттер(ы) класса                                                   |
| `public String toString()`   | перегруженный метод toString() выводит содержание полей класса в строковом виде|

## Класс Fraction (задание 1.1)

### Поля класса Fraction
Fraction имеет 3 поля:
- `private int numerator` - числитель дроби
- `private int denominator` - знаменатель дроби
- `private Double cache` - кэш для вещественного значения

### Конструкторы
Fraction имеет 2 конструктора:
- `public Fraction(int numerator)` - создает дробь с знаменателем 1
- `public Fraction(int numerator, int denominator)` - создает дробь с проверкой знаменателя

### Методы
- `public void reduction()` - сокращает дробь
- `public double getDoubleValue()` - возвращает вещественное значение (с кэшированием)
- `public void setNumerator(int numerator)` - устанавливает числитель
- `public void setDenominator(int denominator)` - устанавливает знаменатель с проверкой
- `private int findGCD(int a, int b)` - находит НОД двух чисел
- `@Override public boolean equals(Object o)` - сравнивает дроби
- `@Override public int hashCode()` - возвращает хэш-код
- `@Override public String toString()` - возвращает строковое представление дроби

## Интерфейс FractionInterface (задание 1.1)

### Методы интерфейса
FractionInterface объявляет 3 метода:
- `double getDoubleValue()` - возвращает вещественное значение
- `void setNumerator(int numerator)` - устанавливает числитель
- `void setDenominator(int denominator)` - устанавливает знаменатель

## Класс Cat (задание 2.1)

### Поля класса Cat
Cat имеет 1 поле:
- `private String name` - имя кота

### Конструктор
Cat имеет 1 конструктор:
- `public Cat(String name)` - создает кота с заданным именем

### Методы
- `public String getName()` - возвращает имя кота
- `public void setName(String name)` - устанавливает имя кота
- `public void meow()` - выводит мяуканье кота
- `@Override public String toString()` - возвращает строковое представление кота

## Интерфейс Meowable (задание 2.1)

### Методы интерфейса
Meowable объявляет 1 метод:
- `void meow()` - метод мяуканья

## Класс MeowCounter (задание 2.1) - декоратор

### Поля класса MeowCounter
MeowCounter имеет 2 поля:
- `private Meowable cat` - декорируемый объект
- `private int meowCount` - счетчик мяуканий

### Конструктор
MeowCounter имеет 1 конструктор:
- `public MeowCounter(Meowable cat)` - создает счетчик для указанного объекта

### Методы
- `@Override public void meow()` - вызывает мяуканье и увеличивает счетчик
- `public int getMeowCount()` - возвращает количество мяуканий

## Класс Meowing (задание 2.1)

### Методы
- `public static void meowing(List<? extends Meowable> meowableList)` - вызывает мяуканье у всех объектов списка

## Класс ReplaceFirstOccurrence (задание 3.2)

### Методы
- `public static <T> void replace(List<T> L, List<T> L1, List<T> L2)` - заменяет первое вхождение списка L1 на список L2 в списке L
- Обрабатывает null списки и исключения

## Класс GasStationAnalyzer (задание 4.7)

### Методы
- `public static void analyzeGasStations(String filename)` - анализирует данные АЗС из файла
- Обрабатывает исключения ввода-вывода и форматирования чисел

## Перечисление GasolineType (задание 4.7)

### Значения
- `TYPE_92(92)` - бензин 92
- `TYPE_95(95)` - бензин 95
- `TYPE_98(98)` - бензин 98

### Методы
- `public int getValue()` - возвращает числовое значение типа
- `public static GasolineType fromInt(int value)` - создает тип из числа

## Класс GasolineInfo (задание 4.7)

### Поля класса
GasolineInfo имеет 2 поля:
- `private int minPrice` - минимальная цена
- `private int stationCount` - количество АЗС

### Конструктор
GasolineInfo имеет 1 конструктор:
- `GasolineInfo(int minPrice, int stationCount)` - создает информацию о бензине

### Методы
- Геттеры и сеттеры для полей
- `public void StationCountPlus1()` - увеличивает счетчик АЗС

## Класс VCAnalyzer (задание 5.2)

### Поля класса
VCAnalyzer имеет статическое поле:
- `private static final Set<Character> VOICELESS_CONSONANTS` - множество глухих согласных

### Методы
- `public static void findMissingVoicelessConsonants(String filename)` - находит отсутствующие согласные в тексте
- `private static void processLine(String line, Set<Character> foundConsonants)` - обрабатывает строку текста
- `private static void printSorted(Set<Character> consonants)` - выводит отсортированный результат

## Класс CircularQueueCheck (задание 6.3)

### Методы
- `public static <T> boolean hasEqualNeighbors(Queue<T> queue)` - проверяет наличие равных соседних элементов в циклической очереди
- Обрабатывает пустые очереди и очереди с одним элементом

## Класс PolylineProcessor (задание 7.1)

### Методы
- `public static Polyline processPoints(List<Point2D> points)` - обрабатывает точки с помощью стримов
- Убирает дубликаты, сортирует по X, делает Y положительными

## Класс Polyline (задание 7.1)

### Поля класса
Polyline имеет 1 поле:
- `private List<Point2D> points` - список точек ломаной

### Конструктор
Polyline имеет 1 конструктор:
- `public Polyline(List<Point2D> points)` - создает ломаную из точек

### Методы
- Геттеры и сеттеры для точек
- `@Override public String toString()` - строковое представление ломаной

## Класс Line (задание 7.1)

### Поля класса
Line имеет 2 поля:
- `private Point2D start` - начальная точка
- `private Point2D finish` - конечная точка

### Конструктор
Line имеет 1 конструктор:
- `public Line(Point2D start, Point2D finish)` - создает линию из двух точек

### Методы
- Геттеры и сеттеры для точек
- `@Override public String toString()` - строковое представление линии

## Класс PeopleProcessor (задание 7.2)

### Методы
- `public static void processPeople(String filename)` - обрабатывает данные людей из файла
- Группирует имена по номерам с помощью стримов
- Обрабатывает исключения ввода-вывода

## Класс Person (задание 7.2)

### Поля класса
Person имеет 2 поля:
- `private String name` - имя человека
- `private int number` - номер человека

### Конструктор
Person имеет 1 конструктор:
- `public Person(String name, int number)` - создает человека с именем и номером

### Методы
- Геттеры и сеттеры для полей
- `@Override public String toString()` - строковое представление

## Тесты
## Задание 1.1 - Дробь с кэшированием

### Тест 1: Создание и вычисление дроби
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Fraction f1 = new Fraction(4, 8); f1.getDoubleValue()` | 0.5 |

### Тест 2: Изменение числителя
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `f1.setNumerator(2); f1.getDoubleValue()` | 0.25 |

### Тест 3: Изменение знаменателя
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `f1.setDenominator(1); f1.getDoubleValue()` | 2.0 |

### Тест 4: Исключение при нулевом знаменателе
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `new Fraction(1, 0)` | IllegalArgumentException |

## Задание 2.1 - Счетчик мяуканий

### Тест 1: Подсчет мяуканий
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Cat cat1 = new Cat("Барсик"); MeowCounter cat1Counter = new MeowCounter(cat1); List.of(cat1Counter, cat1Counter, cat1Counter, cat1Counter, cat1Counter); Meowing.meowing(catList); cat1Counter.getMeowCount()` | 5 |

## Задание 3.2 - Замена первого вхождения

### Тест 1: Замена подсписка
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `List<Integer> l = new ArrayList<>(List.of(0,1,2,3,4,5,1,2,3)); ReplaceFirstOccurrence.replace(l, List.of(1,2,3), List.of(6,7))` | [0, 6, 7, 4, 5, 1, 2, 3] |

## Задание 4.7 - Анализ АЗС

### Тест 1: Обработка файла с данными АЗС
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `GasStationAnalyzer.analyzeGasStations("gas_stations.txt")` | Вывод количества АЗС с минимальной ценой для каждого типа бензина |

## Задание 5.2 - Анализ глухих согласных

### Тест 1: Поиск отсутствующих согласных
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `VCAnalyzer.findMissingVoicelessConsonants("rus_text.txt")` | Вывод глухих согласных, отсутствующих в тексте |

## Задание 6.3 - Проверка циклической очереди

### Тест 1: Очередь без равных соседей
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Queue<Integer> q1 = new LinkedList<>(Arrays.asList(1,2,3,4,5)); CircularQueueCheck.hasEqualNeighbors(q1)` | false |

### Тест 2: Очередь с равными соседями
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Queue<String> q2 = new LinkedList<>(Arrays.asList("a","b","c","c")); CircularQueueCheck.hasEqualNeighbors(q2)` | true |

### Тест 3: Циклическая очередь с равными первым и последним
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `Queue<Integer> q3 = new LinkedList<>(Arrays.asList(1,2,3,4,1)); CircularQueueCheck.hasEqualNeighbors(q3)` | true |

## Задание 7.1 - Обработка точек

### Тест 1: Создание ломаной из точек
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `List<Point2D> points = Arrays.asList(new Point2D(3,4), new Point2D(1,-2), new Point2D(3,4), new Point2D(5,7), new Point2D(2,-5), new Point2D(0,1)); Polyline polyline = PolylineProcessor.processPoints(points)` | Ломаная с уникальными точками, отсортированными по X, с положительными Y |

## Задание 7.2 - Обработка людей

### Тест 1: Группировка людей по номерам
| Входные данные | Ожидаемый результат |
|----------------|---------------------|
| `PeopleProcessor.processPeople("people.txt")` | Вывод сгруппированных имен по номерам |